"""commerce — first business module (assembled from features by the loader).

Features: products (independent), cart (requires products), orders (requires
products). Enabled via ENABLED_MODULES=commerce. See modules/commerce/README.md
for the feature map and build recipes.
"""
