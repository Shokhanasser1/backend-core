"""Business modules (commerce, and later crm/saas). Each module is a folder of
self-contained features assembled by the feature loader (modules/loader.py) when
the module is listed in ENABLED_MODULES. Core never imports modules."""
