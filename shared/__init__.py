"""Shared layer: primitives and base classes below core and modules.

Imports nothing from the project (dependency direction: feature -> core -> shared).
"""

# Template release version. Updated by every template release, delivered to
# client projects via merge and logged at application startup. Never published
# externally (logs and private admin diagnostics only) — see docs/adr/0009.
TEMPLATE_VERSION = "0.3.0"
