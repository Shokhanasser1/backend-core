# Dev-стек одной командой: make dev (docker compose up --build).
# Креды ниже — только для локальной разработки; прод-значения приходят из окружения.

services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: backend
      POSTGRES_PASSWORD: backend
      POSTGRES_DB: backend_core
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
      # Провижининг ролей БД при первом старте (схема §3.1); read-only.
      - ./docker/postgres-init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U backend -d backend_core"]
      interval: 5s
      timeout: 3s
      retries: 10

  redis:
    image: redis:7-alpine
    ports:
      - "127.0.0.1:6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

  api:
    build: .
    environment:
      # Рантайм — app_user (RLS применяется); миграции — app_migrator (владелец).
      DATABASE_URL: postgresql+asyncpg://app_user:app_user@db:5432/backend_core
      DATABASE_MIGRATOR_URL: postgresql+asyncpg://app_migrator:app_migrator@db:5432/backend_core
      DATABASE_MAINTENANCE_URL: postgresql+asyncpg://app_maintenance:app_maintenance@db:5432/backend_core
      REDIS_URL: redis://redis:6379/0
    env_file:
      - path: .env
        required: false
    command: sh -c "python -m migrations.cli upgrade heads && uvicorn app.main:app --host 0.0.0.0 --port 8000"
    ports:
      - "8000:8000"
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy

  worker:
    build: .
    environment:
      DATABASE_URL: postgresql+asyncpg://app_user:app_user@db:5432/backend_core
      DATABASE_MAINTENANCE_URL: postgresql+asyncpg://app_maintenance:app_maintenance@db:5432/backend_core
      REDIS_URL: redis://redis:6379/0
    env_file:
      - path: .env
        required: false
    command: arq app.worker.WorkerSettings
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy

volumes:
  pgdata:
